[patch.crates-io]
# Keep PDF rendering pure Rust while accepting legacy Mac Japanese fonts.
hayro-interpret = { path = "vendor/hayro-interpret" }

[workspace]
members = ["vellum-core", "vellum"]
resolver = "2"

[profile.dev]
opt-level = 2
panic = "abort"

[profile.release]
panic = "abort"
